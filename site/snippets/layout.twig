<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width">
  <title>{{ page.htmltitle | default(page.title ~ ' - ' ~ site.titlesuffix) }}</title>
  <link rel="icon" type="image/x-icon" href="{{ url('/assets/favicon.ico') }}">
  <link rel="stylesheet" href="{{ url('assets/styleguide.css') }}">
</head>
<body{% if bodyClasses|default([]) %} class="{{ bodyClasses|join(' ') }}"{% endif %}>
{% block content %}{% endblock %}
</body>
</html>